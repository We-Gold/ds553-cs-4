name: Build and Deploy to Cloud Run

on:
  push:
    branches:
      - main

env:
  PROJECT_ID: 131429175281
  SERVICE: ds553-service
  REGION: us-central1
  DOCKERHUB_REPO: wegold/ds553

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Authenticate to Google Cloud (Workload Identity Federation)
      - name: Google Auth
        id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: '${{ secrets.WIF_PROVIDER }}'
          service_account: '${{ secrets.WIF_SERVICE_ACCOUNT }}'

      # ---- Docker Hub login ----
    #   - name: Login to Docker Hub
    #     uses: docker/login-action@v3
    #     with:
    #       username: ${{ secrets.DOCKERHUB_USERNAME }}
    #       password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ---- Build and Push Docker image to Docker Hub ----
    #   - name: Build and Push Image
    #     run: |
    #         docker buildx build --platform linux/amd64 -t ${{ env.DOCKERHUB_REPO }}:${{ github.sha }} . --push

      # ---- Deploy to Cloud Run ----
      - name: Deploy to Cloud Run
        id: deploy
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE }}
          region: ${{ env.REGION }}
          image: docker.io/${{ env.DOCKERHUB_REPO }} #:${{ github.sha }}

      - name: Show Output
        run: echo ${{ steps.deploy.outputs.url }}
